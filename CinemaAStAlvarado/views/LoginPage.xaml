<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CinemaAStAlvarado.views.LoginPage">
    <ContentPage.Content>
        <Grid>
            <BoxView Color="#F9C52C" Opacity="0.8"/>

            <Grid Margin="10,10"  VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image Source="login.jpg"/>
                <StackLayout Grid.Row="1" Padding="30,20">
                    <Entry x:Name="txtUsuario" Text="Usuario" PlaceholderColor="White" IsReadOnly="true" />
                    <Entry  x:Name="txtPassword" Text="Password" PlaceholderColor="White"/>
                    <Button  x:Name="btLogin" Clicked="BtLogin_Clicked" Text="Iniciar Sesion" BackgroundColor="Red"/>
                </StackLayout>

            </Grid>
        </Grid>


    </ContentPage.Content>
</ContentPage>